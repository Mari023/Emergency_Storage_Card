pluginManagement {
    repositories {
        gradlePluginPortal()
        maven("https://server.bbkr.space/artifactory/libs-release/")
        maven("https://maven.neoforged.net/releases")
    }
}

rootProject.name = "emergency_storage_card"
